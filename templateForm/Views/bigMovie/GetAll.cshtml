@model IEnumerable<templateForm.Models.movies>

@*<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>GetAll</title>
    <link href="~/Content/bootstrap.css" rel="stylesheet" />
</head>
<body>*@
    <div>
        <table class="table table-dark table-striped">
            <tr>
                <th>id</th>
                <th>name</th>
                <th>year</th>
                <th>rate</th>

            </tr>


            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.id</td>
                    <td>@item.name</td>
                    <td>@item.year</td>
                    <td>@item.rate</td>





                    <td>
                        <a href="/bigMovie/Edit/@item.id" class="btn btn-success">Edit</a> |
                        <a href="/bigMovie/Details/@item.id?id=@item.id" class="btn btn-info">Details</a> |
                        <a href="/bigMovie/Delete/@item.id" class="btn btn-danger" onclick="return confirm('Are u sure !!!')">Delete</a>
                    </td>
                </tr>
            }

        </table>
        <a href="/bigMovie/Create" class="btn btn-primary btn-xl rounded-pill mt-5">create</a>

    </div>
@*</body>
</html>*@
